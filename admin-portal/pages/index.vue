<script setup lang="ts">
</script>

<template>
  <NuxtPage />
  <!-- <main class="h-screen w-screen flex flex-col items-center justify-center space-y-6">
    <h1 class="text-4xl leading-8 font-semibold">
      {{ $t('greeting_title') }}
    </h1>

    <select v-model="language" class="border border-gray-300 px-6 py-4 rounded-md">
      <option
        v-for="item in locales"
        :key="typeof item === 'object' ? item.code : item"
        :value="typeof item === 'object' ? item.code : item"
      >
        {{ typeof item === 'object' ? item.name : item }}
      </option>
    </select>
  </main> -->
</template>
